# Plugin 生態系統整合策略 v2

> Brainstorming 後修正版：evolve Plugin 升級 + skillpkg 合併

**建立日期**: 2026-01-14
**版本**: v2.0（Brainstorming 後修正）

---

## 修正摘要

| 原規劃 | 問題 | 修正 |
|--------|------|------|
| Hooks 用 `echo` | 使用者/Claude 看不到 | 改用 JSON + `additionalContext` |
| 拆分 3 個 Agents | 過度設計，非並行任務 | 移除，保持 Skills 結構 |
| skillpkg 獨立維護 | 官方已覆蓋核心功能 | 合併到 evolve，Archive skillpkg |
| 無 Skill 建立功能 | - | 新增完整 Skill Creator 工作流 |

---

## 一、北極星錨定

### 願景（修正後）

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│   evolve Plugin = 唯一要維護的專案                                  │
│   ════════════════════════════════                                  │
│                                                                     │
│   核心能力：                                                        │
│   ├── 自我進化（現有 /evolve）                                      │
│   ├── CheckPoint 強化（Hooks）                                      │
│   └── Skill 建立工作流（🆕 /evolve --new-skill）                    │
│                                                                     │
│   ❌ skillpkg → Archive（官方已覆蓋）                               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 完成標準

| 項目 | 驗收條件 |
|------|----------|
| **evolve Plugin** | `/plugin install evolve` 一鍵安裝 |
| **Hooks 強化** | CheckPoint 提醒能被 Claude 看到 |
| **Skill Creator** | `/evolve --new-skill` 完整工作流運作 |
| **skillpkg** | Archive，README 說明遷移到 evolve |

### 不做清單

- ❌ 不拆分 Agents（非並行任務）
- ❌ 不獨立維護 skillpkg（合併）
- ❌ 不做跨平台同步（專注 Claude Code）

---

## 二、evolve Plugin 架構設計

### 2.1 目錄結構

```
evolve-plugin/
├── .claude-plugin/
│   └── plugin.json
│
├── commands/
│   ├── evolve/
│   │   └── COMMAND.md              # /evolve [goal]
│   └── new-skill/
│       └── COMMAND.md              # /evolve --new-skill [name]
│
├── skills/
│   ├── 00-getting-started/
│   ├── 01-core/
│   ├── 02-checkpoints/
│   ├── 03-memory/
│   ├── 04-emergence/
│   ├── 05-integration/
│   ├── 06-scaling/
│   ├── 99-evolution/
│   └── skill-creator/              # 🆕 完整工作流
│       ├── SKILL.md
│       ├── templates/
│       │   ├── basic-skill.md
│       │   └── advanced-skill.md
│       └── scripts/
│           ├── validate-skill.sh
│           └── publish-skill.sh
│
├── hooks/
│   ├── checkpoint-reminder.json
│   └── memory-sync.json
│
├── README.md
└── CHANGELOG.md
```

### 2.2 plugin.json

```json
{
  "name": "evolve",
  "description": "Self-evolving agent + Skill creator - 自主學習、持續改進、建立新 Skill",
  "version": "5.0.0",
  "author": {
    "name": "miles990"
  }
}
```

### 2.3 Hooks 配置（修正後）

#### hooks/checkpoint-reminder.json

```json
{
  "event": "PostToolUse",
  "matcher": "Edit|Write",
  "script": "checkpoint-reminder.sh"
}
```

#### hooks/checkpoint-reminder.sh

```bash
#!/bin/bash
# 輸出 JSON 格式，讓 Claude 看到提醒

cat << 'EOF'
{
  "additionalContext": "🔍 CheckPoint 提醒：\n• CP1.5: 確認已檢查現有實作、專案慣例\n• CP2: 記得執行編譯+測試驗證"
}
EOF
```

#### hooks/memory-sync.json

```json
{
  "event": "PostToolUse",
  "matcher": "Write",
  "script": "memory-sync.sh"
}
```

#### hooks/memory-sync.sh

```bash
#!/bin/bash
FILE="$CLAUDE_TOOL_ARG_FILE_PATH"

if [[ "$FILE" == *".claude/memory/"* ]]; then
  cat << 'EOF'
{
  "additionalContext": "📝 CP3.5: 已創建 Memory 文件，記得同步 index.md"
}
EOF
fi
```

---

## 三、Skill Creator 完整工作流

### 3.1 使用方式

```bash
/evolve --new-skill "git commit helper"
```

### 3.2 四階段流程

```
┌─────────────────────────────────────────────────────────────┐
│  Stage 1: 引導式訪談                                        │
│  ─────────────────────                                      │
│  問題：                                                     │
│  1. 這個 skill 要解決什麼問題？                             │
│  2. 目標使用者是誰？（新手/進階/專家）                      │
│  3. 需要什麼前置工具？（MCP servers, CLI tools）            │
│  4. 有沒有參考的 skill？                                    │
│                                                             │
│  輸出：需求文件（內部用）                                   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Stage 2: 分析 + 生成                                       │
│  ───────────────────                                        │
│  動作：                                                     │
│  1. 搜尋 GitHub 上類似的 skills 作為參考                    │
│  2. 選擇適合的範本（basic / advanced）                      │
│  3. 生成 SKILL.md 初稿                                      │
│  4. 建立目錄結構（如需要 scripts/templates）                │
│                                                             │
│  輸出：完整的 skill 目錄                                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Stage 3: 驗證                                              │
│  ───────────                                                │
│  檢查：                                                     │
│  1. SKILL.md frontmatter 格式正確                           │
│  2. 必要欄位存在（name, description）                       │
│  3. 模擬使用情境，確認指令清楚                              │
│  4. 如有 scripts，確認可執行                                │
│                                                             │
│  輸出：驗證報告                                             │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Stage 4: 發布到 GitHub                                     │
│  ──────────────────────                                     │
│  動作：                                                     │
│  1. 詢問：建立新 repo 或加入現有 repo？                     │
│  2. 生成 README.md                                          │
│  3. git init + commit + push                                │
│  4. 輸出安裝指令：                                          │
│     /plugin install <user>/<repo>                           │
│                                                             │
│  輸出：GitHub URL + 安裝指令                                │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 範本設計

#### templates/basic-skill.md

```markdown
---
name: {{name}}
description: {{description}}
version: 1.0.0
---

# {{name}}

> {{description}}

## When to Use

- {{use_case_1}}
- {{use_case_2}}

## Instructions

{{instructions}}

## Examples

\`\`\`
{{example}}
\`\`\`
```

#### templates/advanced-skill.md

```markdown
---
name: {{name}}
description: {{description}}
version: 1.0.0
triggers:
  - {{trigger}}
dependencies:
  mcp:
    - package: "{{mcp_package}}"
---

# {{name}}

> {{description}}

## Overview

{{overview}}

## Prerequisites

- {{prereq_1}}
- {{prereq_2}}

## When to Use

| Scenario | Action |
|----------|--------|
| {{scenario_1}} | {{action_1}} |
| {{scenario_2}} | {{action_2}} |

## Instructions

### Step 1: {{step_1_title}}

{{step_1_content}}

### Step 2: {{step_2_title}}

{{step_2_content}}

## Configuration

\`\`\`json
{{config_example}}
\`\`\`

## Troubleshooting

| Issue | Solution |
|-------|----------|
| {{issue_1}} | {{solution_1}} |
```

---

## 四、實作計劃（修正後）

```
Phase 1: Plugin 結構轉換（2 天）
─────────────────────────────────
□ 建立 evolve-plugin/ 目錄
□ 移動 skills/ 到新結構
□ 建立 .claude-plugin/plugin.json
□ 建立 commands/evolve/COMMAND.md

Phase 2: Hooks 實作（1 天）
───────────────────────────
□ 建立 hooks/checkpoint-reminder.json + .sh
□ 建立 hooks/memory-sync.json + .sh
□ 測試 Hooks 輸出格式（確認 Claude 看得到）

Phase 3: Skill Creator 實作（3 天）
───────────────────────────────────
□ 建立 skills/skill-creator/SKILL.md
□ 建立 templates/（basic + advanced）
□ 建立 scripts/validate-skill.sh
□ 建立 scripts/publish-skill.sh
□ 建立 commands/new-skill/COMMAND.md

Phase 4: 測試 + 發布（1 天）
────────────────────────────
□ 本地測試 /plugin install
□ 測試 /evolve 流程
□ 測試 /evolve --new-skill 流程
□ 發布到 GitHub

Phase 5: skillpkg 收尾（0.5 天）
────────────────────────────────
□ Archive skillpkg repo
□ 更新 README 說明遷移到 evolve
```

---

## 五、風險與緩解

| 風險 | 影響 | 緩解措施 |
|------|------|----------|
| Hooks JSON 格式不正確 | Claude 看不到提醒 | 先測試單一 Hook，確認格式 |
| Skill Creator 流程過長 | 使用者中途放棄 | 每個 Stage 可獨立執行/跳過 |
| Plugin 格式變動 | 需要調整 | 保持結構簡單，追蹤官方更新 |

---

## 六、決策記錄

| 日期 | 決策 | 理由 |
|------|------|------|
| 2026-01-14 | 移除 Agents 拆分 | 非並行任務，過度設計 |
| 2026-01-14 | Hooks 改用 JSON | `echo` 無法被 Claude 看到 |
| 2026-01-14 | skillpkg 合併到 evolve | 官方已覆蓋核心功能 |
| 2026-01-14 | 新增 Skill Creator | 保留 skillpkg 的獨特價值 |

---

## 附錄

### A. Brainstorming 發現

1. **Plugin 安裝範圍**：預設 User scope（全域），不需修正
2. **Hooks 輸出**：需用 JSON + `additionalContext`
3. **Agents 拆分**：過度設計，移除
4. **skillpkg**：官方已覆蓋，合併

### B. 參考資源

- [Claude Code Plugins 官方文檔](https://code.claude.com/docs/en/plugins)
- [Hooks Reference](https://code.claude.com/docs/en/hooks)
- [Skills vs Subagents](https://dev.to/nunc/claude-code-skills-vs-subagents-when-to-use-what-4d12)
